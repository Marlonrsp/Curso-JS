// não podemos criar constantes com palavras reservadas assim como com LET
// as mesmas regras com a vaviavel let vale para a constante (const)
// não podemos modificar o valor de uma constante const

const nome = 'Marlon';// é necessario sempre iniciar a constante com um valor
console.log(nome);


const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
console.log(resultado);
console.log(resultadoDuplicado);